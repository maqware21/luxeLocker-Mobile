import React from 'react';
import { View } from 'react-native';


export default ({ width, height }) => {
  return (
    <View
      style={{
        width: width,
        height: height,
      }}
    />
  );
};
